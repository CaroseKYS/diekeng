## iframe 的 id 和 name 属性

#### 相同点
两个属性都会在全局作用域内产生一个同名变量，当两个属性同时存在且取值相等时，`name` 属性优先。

#### 不同点
 **name属性**：该属性对应的全局变量指向 `iframe` 所引用的子页面的 `window` 对象，该属性也可以作为 `a` 标签的 `target` 属性的取值。

 **id属性**：跟其他元素的 `id` 属性一样，该属性所多应的全局变量指向该 `iframe` 元素。
